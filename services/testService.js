const testModel = require("../models/testModel")

const test = async() => {
    try {
       

    return  true;
    } catch (error) {
        console.log(error)
    }
    
}

module.exports = {
    test
}